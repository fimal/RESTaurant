apiVersion: v1
kind: Service
metadata:
  name: {{ include "apirestaurant.fullname" . }}
  labels:
    {{- include "apirestaurant.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apirestaurant.selectorLabels" . | nindent 4 }}
